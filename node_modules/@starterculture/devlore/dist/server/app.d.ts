import Anthropic from "@anthropic-ai/sdk";
import type { Octokit } from "@octokit/rest";
import { type Express } from "express";
import { type VaultRefreshSettings } from "../lib/vaultRefreshSettings";
import type { SecretStore } from "./secretStore";
import type { VaultState } from "./vaultState";
export declare const SECRET_NAMES: readonly ["anthropic-api-key", "devlore-vault-token"];
export type SecretName = (typeof SECRET_NAMES)[number];
export interface AppCredentials {
    claude: Anthropic | null;
    vaultOctokit: Octokit | null;
}
export interface CreateAppOptions {
    repoRoot: string;
    vaultRepo: {
        owner: string;
        repo: string;
    } | null;
    credentials: AppCredentials;
    vaultState: VaultState;
    secretStore: SecretStore;
    refreshSettings: VaultRefreshSettings;
}
export declare function createApp(options: CreateAppOptions): Express;
