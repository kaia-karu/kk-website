"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createSecretStore = createSecretStore;
const keyring_1 = require("@napi-rs/keyring");
// Requirement #20: the local server's own copy of long-lived secrets
// (Anthropic API key, vault-read token) lives in the OS keychain, not a
// plaintext file or env var. Service name is fixed ("devlore"); account
// name is the secret's logical name (e.g. "anthropic-api-key").
const SERVICE_NAME = "devlore";
function createSecretStore() {
    return {
        getSecret(name) {
            return new keyring_1.Entry(SERVICE_NAME, name).getPassword();
        },
        setSecret(name, value) {
            new keyring_1.Entry(SERVICE_NAME, name).setPassword(value);
        },
    };
}
