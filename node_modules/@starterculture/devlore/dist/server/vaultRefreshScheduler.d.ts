import type { VaultRefreshSettings } from "../lib/vaultRefreshSettings";
export interface RefreshScheduler {
    configure(settings: VaultRefreshSettings): void;
    stop(): void;
}
export declare function createRefreshScheduler(refresh: () => Promise<void>): RefreshScheduler;
