"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRefreshScheduler = createRefreshScheduler;
function createRefreshScheduler(refresh) {
    let timer = null;
    function clear() {
        if (timer) {
            clearInterval(timer);
            timer = null;
        }
    }
    return {
        configure(settings) {
            clear();
            if (settings.mode === "timer") {
                timer = setInterval(() => {
                    // refresh() already records its own failures (vaultState);
                    // this only guards against an unexpected throw taking down
                    // the interval callback.
                    refresh().catch(() => undefined);
                }, settings.timerIntervalMinutes * 60_000);
                // This timer alone should never keep the process alive — the
                // running server's app.listen() already does that. Without this,
                // every "timer mode" instance leaks a live interval that (in
                // tests especially, where nothing ever binds a real listening
                // socket) prevents the process from exiting cleanly.
                timer.unref();
            }
        },
        stop: clear,
    };
}
