import type { Octokit } from "@octokit/rest";
export interface LogEntry {
    path: string;
    content: string;
}
interface ListLogEntriesParams {
    owner: string;
    repo: string;
    projectName: string;
}
export declare function logEntriesDir(projectName: string): string;
export declare function listLogEntries(octokit: Octokit, { owner, repo, projectName }: ListLogEntriesParams): Promise<LogEntry[]>;
export {};
