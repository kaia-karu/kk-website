import type { Octokit } from "@octokit/rest";
export declare const VAULT_BRANCH = "main";
interface VaultRepoParams {
    owner: string;
    repo: string;
}
export interface SyncVaultOptions {
    projectName?: string;
}
export declare function syncVault(octokit: Octokit, { owner, repo }: VaultRepoParams, targetRoot: string, { projectName }?: SyncVaultOptions): Promise<void>;
export {};
