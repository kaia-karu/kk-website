"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CONTINUATION_USER_MESSAGE = void 0;
exports.createCompleteMessage = createCompleteMessage;
const tokenUsage_1 = require("../tokenUsage");
const DEFAULT_MAX_CONTINUATIONS = 5;
// Real bug, found 2026-08-08 via live verification of a sync-test-plan run:
// claude-sonnet-5 rejects the classic Anthropic prefill pattern (partial
// text sent back as a trailing assistant message) with a 400 — "This model
// does not support assistant message prefill. The conversation must end
// with a user message." The partial text still goes back as an assistant
// turn for context, but now followed by this explicit continuation
// request so the conversation ends with a user message, as the model
// requires.
exports.CONTINUATION_USER_MESSAGE = "Continue exactly where you left off. Do not repeat any text already " +
    "written, and do not add any preamble or commentary — resume the " +
    "previous response verbatim from the exact cutoff point.";
// A single Claude call can stop early with stop_reason "max_tokens" if the
// response hasn't finished — silently committing that partial text as
// finished documentation would be worse than the extra API calls this
// costs. Continues the response by sending the partial text back as an
// assistant turn followed by an explicit "continue" request (see
// CONTINUATION_USER_MESSAGE above) until it finishes naturally, or throws
// rather than ever returning truncated content.
//
// usage sums every internal call's tokens, including continuations — not
// double-counting, since Anthropic bills each continuation's full, growing
// context as input tokens again, so this is the real total spend.
async function createCompleteMessage(claude, params, { maxContinuations = DEFAULT_MAX_CONTINUATIONS, timeoutMs, signal, } = {}) {
    let fullText = "";
    let usage = tokenUsage_1.ZERO_USAGE;
    // Real incident, 2026-08-17: a chat request's underlying Claude call had
    // no configured request timeout, so a dropped/stalled connection (as
    // opposed to Claude genuinely taking a while) could hang forever with no
    // way to ever surface as a failure. Both timeoutMs and signal are opt-in
    // and per-call (applied to every continuation attempt, not the loop as a
    // whole) — omitted entirely, not passed as `undefined`, so every
    // existing caller that doesn't pass them (the CI action scripts, several
    // of which use much larger max_tokens budgets and can legitimately take
    // longer per call than chat) sees the exact same request shape as
    // before, falling back to the SDK's own default timeout unchanged.
    const requestOptions = timeoutMs !== undefined || signal !== undefined ? { timeout: timeoutMs, signal } : undefined;
    for (let attempt = 0; attempt <= maxContinuations; attempt++) {
        const messages = fullText
            ? [
                ...params.messages,
                { role: "assistant", content: fullText },
                { role: "user", content: exports.CONTINUATION_USER_MESSAGE },
            ]
            : params.messages;
        // A genuinely omitted second argument, not `create(params, undefined)`
        // — those aren't the same call as far as an exact-arguments assertion
        // (e.g. Jest's toHaveBeenCalledWith on the existing CI action script
        // tests) is concerned, even though the SDK treats them identically.
        const message = requestOptions
            ? await claude.messages.create({ ...params, messages }, requestOptions)
            : await claude.messages.create({ ...params, messages });
        const text = message.content
            .filter((block) => block.type === "text")
            .map((block) => block.text)
            .join("");
        fullText += text;
        usage = (0, tokenUsage_1.addTokenUsage)(usage, {
            inputTokens: message.usage?.input_tokens ?? 0,
            outputTokens: message.usage?.output_tokens ?? 0,
        });
        if (message.stop_reason !== "max_tokens") {
            return { text: fullText, usage };
        }
    }
    throw new Error(`Claude response still hit max_tokens after ${maxContinuations} continuation attempts — refusing to return truncated content.`);
}
