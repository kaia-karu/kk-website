import type Anthropic from "@anthropic-ai/sdk";
import type { MessageCreateParamsNonStreaming } from "@anthropic-ai/sdk/resources/messages";
import { type TokenUsage } from "../tokenUsage";
export declare const CONTINUATION_USER_MESSAGE: string;
export interface CompleteMessageResult {
    text: string;
    usage: TokenUsage;
}
export declare function createCompleteMessage(claude: Anthropic, params: MessageCreateParamsNonStreaming, { maxContinuations, timeoutMs, signal, }?: {
    maxContinuations?: number;
    timeoutMs?: number;
    signal?: AbortSignal;
}): Promise<CompleteMessageResult>;
