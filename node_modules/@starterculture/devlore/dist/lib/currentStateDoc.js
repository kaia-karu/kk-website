"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.currentStateDocPath = currentStateDocPath;
exports.getCurrentStateDoc = getCurrentStateDoc;
// Requirement #34: baseline codebase-analysis output, one file per
// project, closer in shape to masterDocs.ts (single reference file) than
// logEntries.ts (a growing directory) — there's only ever one current
// snapshot, overwritten by each re-dispatch, not accumulated over time.
function currentStateDocPath(projectName) {
    return `projects/${projectName}/current-state.md`;
}
async function getCurrentStateDoc(octokit, { owner, repo, projectName }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: currentStateDocPath(projectName),
        });
        if (Array.isArray(data) || data.type !== "file") {
            return { content: "" };
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return { content: "" };
        }
        throw error;
    }
}
function isNotFound(error) {
    return (typeof error === "object" &&
        error !== null &&
        "status" in error &&
        error.status === 404);
}
