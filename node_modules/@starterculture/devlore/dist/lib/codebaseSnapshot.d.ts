export interface CodebaseSnapshot {
    tree: string[];
    files: {
        path: string;
        content: string;
    }[];
}
export declare function buildCodebaseSnapshot(rootDir: string): Promise<CodebaseSnapshot>;
