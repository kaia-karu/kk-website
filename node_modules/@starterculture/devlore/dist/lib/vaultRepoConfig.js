"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.vaultRepoConfigPath = vaultRepoConfigPath;
exports.readVaultRepoConfig = readVaultRepoConfig;
exports.writeVaultRepoConfig = writeVaultRepoConfig;
const promises_1 = require("fs/promises");
const path_1 = __importDefault(require("path"));
function vaultRepoConfigPath(repoRoot) {
    return path_1.default.join(repoRoot, ".devlore", "vault-repo.json");
}
async function readVaultRepoConfig(repoRoot) {
    try {
        const raw = await (0, promises_1.readFile)(vaultRepoConfigPath(repoRoot), "utf-8");
        return JSON.parse(raw);
    }
    catch (error) {
        if (isNotFound(error)) {
            return null;
        }
        throw error;
    }
}
async function writeVaultRepoConfig(repoRoot, config) {
    const filePath = vaultRepoConfigPath(repoRoot);
    await (0, promises_1.mkdir)(path_1.default.dirname(filePath), { recursive: true });
    await (0, promises_1.writeFile)(filePath, JSON.stringify(config, null, 2), "utf-8");
}
function isNotFound(error) {
    return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
