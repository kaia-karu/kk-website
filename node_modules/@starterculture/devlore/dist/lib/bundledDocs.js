"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readBundledUserManual = readBundledUserManual;
const promises_1 = require("fs/promises");
const path_1 = __importDefault(require("path"));
// Devlore's own User Manual (docs/USER_MANUAL.md) only has real vault
// content for the maintainer's self-linked instance — every other install's
// vault has no "devlore" project in it at all. The build step (see
// package.json's "sync:assets", wired into "prebuild"/"preserve") copies
// that file into assets/user-manual.md, shipped alongside dist/ and public/
// in the published package, so the User Manual pane still renders for fresh
// installs. Same __dirname depth as app.ts's public/ resolution: this file
// sits at src/lib/ (or dist/lib/ once compiled), two levels above the repo
// root.
async function readBundledUserManual() {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(__dirname, "../../assets/user-manual.md"), "utf-8");
    }
    catch {
        return "";
    }
}
