"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.chatUsagePath = chatUsagePath;
exports.readChatUsage = readChatUsage;
exports.recordChatUsage = recordChatUsage;
const promises_1 = require("fs/promises");
const path_1 = __importDefault(require("path"));
const tokenUsage_1 = require("./tokenUsage");
function chatUsagePath(repoRoot) {
    return path_1.default.join(repoRoot, ".devlore", "chat-usage.json");
}
async function readChatUsage(repoRoot) {
    try {
        const raw = await (0, promises_1.readFile)(chatUsagePath(repoRoot), "utf-8");
        return JSON.parse(raw);
    }
    catch (error) {
        if (isNotFound(error)) {
            return {};
        }
        throw error;
    }
}
// Read-modify-write, not atomic across concurrent requests — lower stakes
// than the CI-side equivalent (single local dev server process), so a lost
// update in a race just means a slightly undercounted local number.
async function recordChatUsage(repoRoot, projectName, delta) {
    const current = await readChatUsage(repoRoot);
    const updated = {
        ...current,
        [projectName]: (0, tokenUsage_1.addTokenUsage)(current[projectName] ?? tokenUsage_1.ZERO_USAGE, delta),
    };
    const filePath = chatUsagePath(repoRoot);
    await (0, promises_1.mkdir)(path_1.default.dirname(filePath), { recursive: true });
    await (0, promises_1.writeFile)(filePath, JSON.stringify(updated, null, 2), "utf-8");
    return updated;
}
function isNotFound(error) {
    return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
