"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.redactText = redactText;
exports.isSensitiveFilename = isSensitiveFilename;
exports.redactDiffFile = redactDiffFile;
exports.redactProjectDiff = redactProjectDiff;
const REDACTED = "[REDACTED]";
// Known secret formats — matched and replaced wholesale wherever they appear,
// regardless of surrounding context.
const KNOWN_SECRET_PATTERNS = [
    /-----BEGIN [A-Z ]*PRIVATE KEY-----[\s\S]*?-----END [A-Z ]*PRIVATE KEY-----/g,
    /gh[pousr]_[A-Za-z0-9]{36,}/g,
    /github_pat_[A-Za-z0-9_]{20,}/g,
    /sk-ant-[A-Za-z0-9_-]{20,}/g,
    /sk-[A-Za-z0-9]{20,}/g,
    /AKIA[0-9A-Z]{16}/g,
    /xox[baprs]-[A-Za-z0-9-]{10,}/g,
    /eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}/g,
];
// Assignment-style secrets: `KEY=value`, `key: value`, `"key": "value"`,
// wherever the key name suggests it holds a secret. Only the value is
// redacted so the diff stays readable.
//
// Real bug, found 2026-08-08 via a live vault-sync check: the previous
// version allowed arbitrary letters directly before/after the keyword
// (`[A-Za-z0-9_-]*TOKEN[A-Za-z0-9_-]*`), which matches "TOKEN" as a bare
// substring of ordinary words — "max_tokens" (a real, extremely common
// Claude API parameter, mentioned throughout this repo's own docs) got
// silently mangled into "max_tokens: [REDACTED]" every time docs/PRODUCT.md
// was mirrored to the vault. Segments around the keyword now require a
// `_`/`-` separator (GITHUB_TOKEN, API_KEY_ID still match; "tokens",
// "tokenizer" no longer do, since the extra letters aren't separated).
// The value class also now excludes backticks, not just quotes — the old
// version would swallow a closing markdown backtick as part of the
// "secret value", which is how the literal example text `` `API_KEY=` ``
// in this file's own Requirement #17 write-up got corrupted into
// `` `API_KEY=[REDACTED] `` with the closing backtick eaten.
const SECRET_KEYWORD = "(?:API[_-]?KEY|SECRET|TOKEN|PASSWORD|PASSWD|PRIVATE[_-]?KEY|CREDENTIAL)";
const ASSIGNMENT_PATTERN = new RegExp(`((?:[A-Za-z0-9]+[_-])*${SECRET_KEYWORD}(?:[_-][A-Za-z0-9]+)*\\s*[:=]\\s*)(["']?)([^"'\`\\s]+)\\2`, "gi");
// Files whose entire contents are assumed sensitive — redact the whole
// patch rather than relying on pattern matching to catch every case.
const SENSITIVE_FILENAME_PATTERNS = [
    /(^|\/)\.env(\..+)?$/,
    /\.pem$/,
    /\.key$/,
    /\.p12$/,
    /\.pfx$/,
    /(^|\/)id_rsa(\.pub)?$/,
    /(^|\/)id_ed25519(\.pub)?$/,
    /credentials\.json$/,
    /secrets?\.(json|ya?ml|toml)$/,
];
function redactText(text) {
    let result = text;
    for (const pattern of KNOWN_SECRET_PATTERNS) {
        result = result.replace(pattern, REDACTED);
    }
    result = result.replace(ASSIGNMENT_PATTERN, (_match, prefix, quote) => `${prefix}${quote}${REDACTED}${quote}`);
    return result;
}
function isSensitiveFilename(filename) {
    return SENSITIVE_FILENAME_PATTERNS.some((pattern) => pattern.test(filename));
}
function redactDiffFile(file) {
    if (isSensitiveFilename(file.filename)) {
        return { ...file, patch: `(patch redacted: ${file.filename} may contain secrets)` };
    }
    return { ...file, patch: redactText(file.patch) };
}
function redactProjectDiff(diff) {
    return {
        commitMessages: diff.commitMessages.map(redactText),
        files: diff.files.map(redactDiffFile),
    };
}
