"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EMPTY_PROJECT_USAGE = void 0;
exports.usagePath = usagePath;
exports.getUsage = getUsage;
exports.getUsageSha = getUsageSha;
exports.addUsage = addUsage;
exports.recordUsage = recordUsage;
const tokenUsage_1 = require("./tokenUsage");
const retryOnConflict_1 = require("./github/retryOnConflict");
exports.EMPTY_PROJECT_USAGE = {
    draftLogEntry: tokenUsage_1.ZERO_USAGE,
    consolidateRelease: tokenUsage_1.ZERO_USAGE,
    analyzeCodebase: tokenUsage_1.ZERO_USAGE,
    syncTestPlan: tokenUsage_1.ZERO_USAGE,
    syncUserManual: tokenUsage_1.ZERO_USAGE,
    releaseNotes: tokenUsage_1.ZERO_USAGE,
    visualizer: tokenUsage_1.ZERO_USAGE,
    onboardingGuide: tokenUsage_1.ZERO_USAGE,
};
function usagePath(projectName) {
    return `projects/${projectName}/usage.json`;
}
async function getUsage(octokit, params) {
    return (await getUsageFile(octokit, params)).usage;
}
async function getUsageSha(octokit, params) {
    return (await getUsageFile(octokit, params)).sha;
}
async function getUsageFile(octokit, { owner, repo, projectName }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: usagePath(projectName),
        });
        if (Array.isArray(data) || data.type !== "file") {
            return { usage: exports.EMPTY_PROJECT_USAGE };
        }
        // Merged over EMPTY_PROJECT_USAGE, not trusted as complete on its
        // own — a usage.json written before a new source existed (e.g.
        // analyzeCodebase, Requirement #34) won't have that key yet, and
        // addUsage()/addTokenUsage() have no null-check of their own. Caught
        // for real: a live analyze-codebase run crashed against a real
        // project's pre-existing, pre-Requirement-#34 usage.json.
        const parsed = JSON.parse(Buffer.from(data.content, "base64").toString("utf-8"));
        const usage = { ...exports.EMPTY_PROJECT_USAGE, ...parsed };
        return { usage, sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return { usage: exports.EMPTY_PROJECT_USAGE };
        }
        throw error;
    }
}
function addUsage(current, source, delta) {
    return { ...current, [source]: (0, tokenUsage_1.addTokenUsage)(current[source], delta) };
}
// Read-modify-write, not atomic across concurrent runs for the same
// project — draft-log-entry and sync-test-plan both record usage against
// the same usage.json on every push, and the wizard's rapid multi-commit
// linking sequence can run both concurrently. retryOnConflict re-reads
// current+sha on each attempt, so a 409 just means "redo the merge against
// whatever's there now" rather than losing the delta.
async function recordUsage(octokit, params, source, delta) {
    const { owner, repo, projectName, branch } = params;
    await (0, retryOnConflict_1.retryOnConflict)(async () => {
        const { usage: current, sha } = await getUsageFile(octokit, { owner, repo, projectName });
        const updated = addUsage(current, source, delta);
        await octokit.rest.repos.createOrUpdateFileContents({
            owner,
            repo,
            path: usagePath(projectName),
            message: `Record ${source} token usage: ${projectName}`,
            content: Buffer.from(JSON.stringify(updated, null, 2), "utf-8").toString("base64"),
            branch,
            sha,
        });
    });
}
function isNotFound(error) {
    return (typeof error === "object" &&
        error !== null &&
        "status" in error &&
        error.status === 404);
}
