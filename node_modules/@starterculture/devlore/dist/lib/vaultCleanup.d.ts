import type { Octokit } from "@octokit/rest";
interface RepoRef {
    owner: string;
    repo: string;
}
export declare function deleteVaultProject(octokit: Octokit, { owner, repo }: RepoRef, projectName: string): Promise<void>;
export {};
