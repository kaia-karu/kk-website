"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAppDataDir = getAppDataDir;
exports.getVaultCacheDir = getVaultCacheDir;
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
const APP_NAME = "devlore";
function getAppDataDir(options = {}) {
    const platform = options.platform ?? process.platform;
    const env = options.env ?? process.env;
    const homedir = options.homedir ?? os_1.default.homedir();
    const p = platform === "win32" ? path_1.default.win32 : path_1.default.posix;
    // Test/automation-only escape hatch (see playwright.config.ts) — same
    // precedent as DEVLORE_SKIP_KEYCHAIN, never a supported end-user
    // option. Without it, e2e's spawned server shares this exact directory
    // with real `npm run serve` usage: a real bug once it actually bit
    // (see git history) — an e2e run's placeholder VAULT_REPO got persisted
    // into the real, shared vault-repo.json, silently overriding the real
    // .env value on every subsequent real launch until manually repaired.
    if (env.DEVLORE_APP_DATA_DIR) {
        return env.DEVLORE_APP_DATA_DIR;
    }
    if (platform === "darwin") {
        return p.join(homedir, "Library", "Application Support", APP_NAME);
    }
    if (platform === "win32") {
        const appData = env.APPDATA ?? p.join(homedir, "AppData", "Roaming");
        return p.join(appData, APP_NAME);
    }
    const xdgDataHome = env.XDG_DATA_HOME ?? p.join(homedir, ".local", "share");
    return p.join(xdgDataHome, APP_NAME);
}
function getVaultCacheDir(options = {}) {
    const platform = options.platform ?? process.platform;
    const p = platform === "win32" ? path_1.default.win32 : path_1.default.posix;
    return p.join(getAppDataDir(options), "vault");
}
