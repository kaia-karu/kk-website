"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hashRequirementsSection = exports.VISUALIZER_PATH = void 0;
exports.visualizerBanner = visualizerBanner;
exports.extractSourceHash = extractSourceHash;
exports.getExistingVisualizer = getExistingVisualizer;
exports.visualizerVaultPath = visualizerVaultPath;
exports.getDevloreVisualizer = getDevloreVisualizer;
const testPlanDoc_1 = require("./testPlanDoc");
Object.defineProperty(exports, "hashRequirementsSection", { enumerable: true, get: function () { return testPlanDoc_1.hashRequirementsSection; } });
// Requirement #12: docs/VISUALIZER.md is generated per linked project,
// one or more Mermaid diagrams showing how the repo's pieces fit
// together — internal modules, external APIs it calls, and (where
// evident from the docs) other linked repos. Fully machine-owned, same
// convention as docs/TEST_PLAN.md/docs/USER_MANUAL.md: the banner
// protects both the path and the content, not just the path the way
// docs/PRODUCT.md's does. Regenerated on the same drift signal as those
// two (a hash of docs/PRODUCT.md's Requirements section) rather than a
// bespoke "did the codebase structure change" signal — requirements
// changes are a reasonable, already-proven proxy for "the diagram might
// be stale now," and reusing it keeps this a same-shaped feature instead
// of a new detection mechanism to build and trust.
exports.VISUALIZER_PATH = "docs/VISUALIZER.md";
const VISUALIZER_MARKER_PATTERN = /<!-- devlore:visualizer source-hash:([a-f0-9]+) -->/;
function visualizerBanner(sourceHash) {
    return `<!-- devlore:visualizer source-hash:${sourceHash} -->
> **Do not move, rename, or edit this file.** Devlore generates and maintains this diagram automatically from \`docs/PRODUCT.md\`'s requirements — manual edits will be overwritten the next time Devlore detects the requirements have changed. To change what's diagrammed, update \`docs/PRODUCT.md\` itself.`;
}
function extractSourceHash(content) {
    const match = content.match(VISUALIZER_MARKER_PATTERN);
    return match ? match[1] : null;
}
// Returns null when missing (first-ever generation), not an empty-content
// sentinel — same reasoning as getExistingTestPlan: sync-visualizer.ts
// needs to tell "doesn't exist yet" apart from "exists but its hash
// doesn't match."
async function getExistingVisualizer(octokit, { owner, repo }) {
    try {
        const { data } = await octokit.rest.repos.getContent({ owner, repo, path: exports.VISUALIZER_PATH });
        if (Array.isArray(data) || data.type !== "file") {
            return null;
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return null;
        }
        throw error;
    }
}
// Mirrors productDoc.ts's productDocPath/getDevloreProductDoc — the local
// chat server needs a copy of the diagram in the vault so it can serve
// the "Visualizer" tab straight off disk (Requirement #11), the same way
// it already does for the test plan and user manual.
function visualizerVaultPath(projectName) {
    return `projects/${projectName}/visualizer.md`;
}
async function getDevloreVisualizer(octokit, { owner, repo, projectName }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: visualizerVaultPath(projectName),
        });
        if (Array.isArray(data) || data.type !== "file") {
            return { content: "" };
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return { content: "" };
        }
        throw error;
    }
}
function isNotFound(error) {
    return (typeof error === "object" &&
        error !== null &&
        "status" in error &&
        error.status === 404);
}
