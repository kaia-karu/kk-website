"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.listProjectNames = listProjectNames;
exports.readTestPlanDoc = readTestPlanDoc;
exports.readUserManualDoc = readUserManualDoc;
exports.readVisualizerDoc = readVisualizerDoc;
exports.readOnboardingDoc = readOnboardingDoc;
exports.readReleaseNotes = readReleaseNotes;
exports.readProjectDocs = readProjectDocs;
exports.loadAllProjectDocs = loadAllProjectDocs;
const promises_1 = require("fs/promises");
const path_1 = __importDefault(require("path"));
const ciUsage_1 = require("./ciUsage");
const currentStateDoc_1 = require("./currentStateDoc");
const decisionEntries_1 = require("./decisionEntries");
const logEntries_1 = require("./logEntries");
const masterDocs_1 = require("./masterDocs");
const onboardingDoc_1 = require("./onboardingDoc");
const productDoc_1 = require("./productDoc");
const testPlanDoc_1 = require("./testPlanDoc");
const userManualDoc_1 = require("./userManualDoc");
const visualizerDoc_1 = require("./visualizerDoc");
const releaseNotesDoc_1 = require("./releaseNotesDoc");
function isNotFound(error) {
    return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
async function listProjectNames(repoRoot) {
    let entries;
    try {
        entries = await (0, promises_1.readdir)(path_1.default.join(repoRoot, "projects"), { withFileTypes: true });
    }
    catch (error) {
        if (isNotFound(error)) {
            return [];
        }
        throw error;
    }
    return entries
        .filter((entry) => entry.isDirectory())
        .map((entry) => entry.name)
        .sort((a, b) => a.localeCompare(b));
}
async function readMasterDocs(repoRoot, projectName) {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, masterDocs_1.masterDocsPath)(projectName)), "utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return "";
        }
        throw error;
    }
}
async function readProductDoc(repoRoot, projectName) {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, productDoc_1.productDocPath)(projectName)), "utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return "";
        }
        throw error;
    }
}
// Standalone export (not private like the other read* helpers above) —
// the "Test plan" tab's dedicated endpoint needs just this one doc, not
// the full readProjectDocs() bundle (log entries, master docs, etc.) that
// exists to feed the chat system prompt.
async function readTestPlanDoc(repoRoot, projectName) {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, testPlanDoc_1.testPlanVaultPath)(projectName)), "utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return "";
        }
        throw error;
    }
}
// Standalone export, same reasoning as readTestPlanDoc above — the
// "User Manual" pane's dedicated endpoint needs just this one doc.
async function readUserManualDoc(repoRoot, projectName) {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, userManualDoc_1.userManualVaultPath)(projectName)), "utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return "";
        }
        throw error;
    }
}
// Standalone export, same reasoning as readTestPlanDoc above — the
// "Visualizer" tab's dedicated endpoint needs just this one doc.
async function readVisualizerDoc(repoRoot, projectName) {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, visualizerDoc_1.visualizerVaultPath)(projectName)), "utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return "";
        }
        throw error;
    }
}
// Standalone export, same reasoning as readTestPlanDoc above — the
// "Onboarding" tab's dedicated endpoint needs just this one doc.
async function readOnboardingDoc(repoRoot, projectName) {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, onboardingDoc_1.onboardingVaultPath)(projectName)), "utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return "";
        }
        throw error;
    }
}
async function readCurrentStateDoc(repoRoot, projectName) {
    try {
        return await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, currentStateDoc_1.currentStateDocPath)(projectName)), "utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return "";
        }
        throw error;
    }
}
async function readCiUsage(repoRoot, projectName) {
    try {
        const raw = await (0, promises_1.readFile)(path_1.default.join(repoRoot, (0, ciUsage_1.usagePath)(projectName)), "utf-8");
        // Merged over EMPTY_PROJECT_USAGE, not trusted as a complete
        // ProjectUsage on its own — a usage.json written before a new source
        // was added (e.g. analyzeCodebase, Requirement #34) won't have that
        // key yet, and addTokenUsage() has no null-check of its own.
        return { ...ciUsage_1.EMPTY_PROJECT_USAGE, ...JSON.parse(raw) };
    }
    catch (error) {
        if (isNotFound(error)) {
            return ciUsage_1.EMPTY_PROJECT_USAGE;
        }
        throw error;
    }
}
async function readLogEntries(repoRoot, projectName) {
    const dir = (0, logEntries_1.logEntriesDir)(projectName);
    let filenames;
    try {
        filenames = await (0, promises_1.readdir)(path_1.default.join(repoRoot, dir));
    }
    catch (error) {
        if (isNotFound(error)) {
            return [];
        }
        throw error;
    }
    const entries = await Promise.all(filenames
        .filter((filename) => filename.endsWith(".md"))
        .map(async (filename) => {
        const entryPath = `${dir}/${filename}`;
        const content = await (0, promises_1.readFile)(path_1.default.join(repoRoot, entryPath), "utf-8");
        return { path: entryPath, content };
    }));
    return entries.sort((a, b) => a.path.localeCompare(b.path));
}
// Same shape/sort as readLogEntries above — one file per push that
// cleared the decision bar under projects/<name>/decisions/, read the
// same ENOENT-tolerant way. See decisionEntries.ts for why this folder
// used to always be empty.
async function readDecisionEntries(repoRoot, projectName) {
    const dir = (0, decisionEntries_1.decisionEntriesDir)(projectName);
    let filenames;
    try {
        filenames = await (0, promises_1.readdir)(path_1.default.join(repoRoot, dir));
    }
    catch (error) {
        if (isNotFound(error)) {
            return [];
        }
        throw error;
    }
    const entries = await Promise.all(filenames
        .filter((filename) => filename.endsWith(".md"))
        .map(async (filename) => {
        const entryPath = `${dir}/${filename}`;
        const content = await (0, promises_1.readFile)(path_1.default.join(repoRoot, entryPath), "utf-8");
        return { path: entryPath, content };
    }));
    return entries.sort((a, b) => a.path.localeCompare(b.path));
}
// Same shape/sort as readLogEntries above — one file per release tag
// under projects/<name>/releases/, read the same ENOENT-tolerant way.
// Standalone export, same reasoning as readTestPlanDoc above — the
// "Release notes" tab's dedicated endpoint needs just these, not the
// full readProjectDocs() bundle.
async function readReleaseNotes(repoRoot, projectName) {
    const dir = (0, releaseNotesDoc_1.releaseNotesDir)(projectName);
    let filenames;
    try {
        filenames = await (0, promises_1.readdir)(path_1.default.join(repoRoot, dir));
    }
    catch (error) {
        if (isNotFound(error)) {
            return [];
        }
        throw error;
    }
    const entries = await Promise.all(filenames
        .filter((filename) => filename.endsWith(".md"))
        .map(async (filename) => {
        const entryPath = `${dir}/${filename}`;
        const content = await (0, promises_1.readFile)(path_1.default.join(repoRoot, entryPath), "utf-8");
        return { path: entryPath, content };
    }));
    return entries.sort((a, b) => a.path.localeCompare(b.path));
}
async function readProjectDocs(repoRoot, projectName) {
    const [currentStateDoc, productDoc, testPlanDoc, userManualDoc, visualizerDoc, onboardingDoc, masterDocs, logEntries, decisions, releaseNotes, ciUsage,] = await Promise.all([
        readCurrentStateDoc(repoRoot, projectName),
        readProductDoc(repoRoot, projectName),
        readTestPlanDoc(repoRoot, projectName),
        readUserManualDoc(repoRoot, projectName),
        readVisualizerDoc(repoRoot, projectName),
        readOnboardingDoc(repoRoot, projectName),
        readMasterDocs(repoRoot, projectName),
        readLogEntries(repoRoot, projectName),
        readDecisionEntries(repoRoot, projectName),
        readReleaseNotes(repoRoot, projectName),
        readCiUsage(repoRoot, projectName),
    ]);
    return {
        name: projectName,
        currentStateDoc,
        productDoc,
        testPlanDoc,
        userManualDoc,
        visualizerDoc,
        onboardingDoc,
        masterDocs,
        logEntries,
        decisions,
        releaseNotes,
        ciUsage,
    };
}
async function loadAllProjectDocs(repoRoot) {
    const projectNames = await listProjectNames(repoRoot);
    return Promise.all(projectNames.map((name) => readProjectDocs(repoRoot, name)));
}
