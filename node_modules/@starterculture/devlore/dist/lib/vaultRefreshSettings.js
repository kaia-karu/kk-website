"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_VAULT_REFRESH_SETTINGS = void 0;
exports.vaultRefreshSettingsPath = vaultRefreshSettingsPath;
exports.readVaultRefreshSettings = readVaultRefreshSettings;
exports.writeVaultRefreshSettings = writeVaultRefreshSettings;
const promises_1 = require("fs/promises");
const path_1 = __importDefault(require("path"));
exports.DEFAULT_VAULT_REFRESH_SETTINGS = {
    mode: "per-query",
    timerIntervalMinutes: 5,
};
function vaultRefreshSettingsPath(repoRoot) {
    return path_1.default.join(repoRoot, ".devlore", "vault-refresh-settings.json");
}
async function readVaultRefreshSettings(repoRoot) {
    try {
        const raw = await (0, promises_1.readFile)(vaultRefreshSettingsPath(repoRoot), "utf-8");
        return JSON.parse(raw);
    }
    catch (error) {
        if (isNotFound(error)) {
            return exports.DEFAULT_VAULT_REFRESH_SETTINGS;
        }
        throw error;
    }
}
async function writeVaultRefreshSettings(repoRoot, settings) {
    const filePath = vaultRefreshSettingsPath(repoRoot);
    await (0, promises_1.mkdir)(path_1.default.dirname(filePath), { recursive: true });
    await (0, promises_1.writeFile)(filePath, JSON.stringify(settings, null, 2), "utf-8");
}
function isNotFound(error) {
    return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
