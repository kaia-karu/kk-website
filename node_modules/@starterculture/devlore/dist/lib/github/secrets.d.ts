import type { Octokit } from "@octokit/rest";
interface RepoRef {
    owner: string;
    repo: string;
}
export interface EncryptedSecret {
    encryptedValue: string;
    keyId: string;
}
export declare function encryptRepoSecret(octokit: Octokit, { owner, repo }: RepoRef, value: string): Promise<EncryptedSecret>;
export {};
