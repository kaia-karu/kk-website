export declare function describeWizardGithubError(error: unknown, fallback: string): string;
export declare function describeVaultSyncError(error: unknown, vaultRepo: {
    owner: string;
    repo: string;
}): Promise<string>;
