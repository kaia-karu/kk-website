"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.encryptRepoSecret = encryptRepoSecret;
const libsodium_wrappers_1 = __importDefault(require("libsodium-wrappers"));
async function encryptRepoSecret(octokit, { owner, repo }, value) {
    const { data } = await octokit.rest.actions.getRepoPublicKey({ owner, repo });
    await libsodium_wrappers_1.default.ready;
    const publicKeyBytes = libsodium_wrappers_1.default.from_base64(data.key, libsodium_wrappers_1.default.base64_variants.ORIGINAL);
    // Deliberately not using crypto_box_seal's own "base64" outputFormat —
    // confirmed for real against GitHub's API that it produces a
    // URL-safe/no-padding variant, which GitHub's strict standard-base64
    // validation on encrypted_value rejects. Get raw bytes instead and
    // encode with Node's own (standard, padded) base64.
    const sealedBytes = libsodium_wrappers_1.default.crypto_box_seal(value, publicKeyBytes);
    const encryptedValue = Buffer.from(sealedBytes).toString("base64");
    return { encryptedValue, keyId: data.key_id };
}
