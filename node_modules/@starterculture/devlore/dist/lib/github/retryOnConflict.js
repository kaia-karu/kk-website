"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.retryOnConflict = retryOnConflict;
// GitHub's Contents API returns 409/422 when two writes race the same
// branch — one run's read of "current state" goes stale the instant a
// concurrent run commits first (see draft-log-entry.ts and
// sync-test-plan.ts, both invoked on every push, so the wizard's rapid
// multi-commit linking sequence can trigger several runs at once).
// Retrying re-invokes the whole passed-in attempt, so a caller that reads
// state (e.g. a file's current sha) before writing should do that read
// inside `attempt` too — otherwise a retry just repeats the same stale
// write and fails identically.
async function retryOnConflict(attempt, maxAttempts = 3) {
    for (let tryNumber = 1;; tryNumber++) {
        try {
            return await attempt();
        }
        catch (error) {
            const status = error.status;
            if (tryNumber >= maxAttempts || (status !== 409 && status !== 422)) {
                throw error;
            }
            await new Promise((resolve) => setTimeout(resolve, 300 * tryNumber));
        }
    }
}
