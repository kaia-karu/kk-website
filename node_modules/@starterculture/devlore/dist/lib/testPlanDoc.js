"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TEST_PLAN_PATH = void 0;
exports.extractRequirementsSection = extractRequirementsSection;
exports.hashRequirementsSection = hashRequirementsSection;
exports.testPlanBanner = testPlanBanner;
exports.extractSourceHash = extractSourceHash;
exports.getExistingTestPlan = getExistingTestPlan;
exports.testPlanVaultPath = testPlanVaultPath;
exports.getDevloreTestPlan = getDevloreTestPlan;
const crypto_1 = require("crypto");
// Requirement #35: docs/TEST_PLAN.md is generated per linked project from
// that project's own docs/PRODUCT.md requirements, fully machine-owned
// (unlike docs/PRODUCT.md itself — see productDoc.ts's banner, which
// protects the path but explicitly encourages editing the content; this
// one protects both). Lives in the project repo, not the vault, since a
// QA engineer needs to read it directly on GitHub.
exports.TEST_PLAN_PATH = "docs/TEST_PLAN.md";
const REQUIREMENTS_HEADING = "## Requirements";
const TEST_PLAN_MARKER_PATTERN = /<!-- devlore:test-plan source-hash:([a-f0-9]+) -->/;
// Only the ## Requirements section, not the whole file — narrative
// sections (Vision, Legal & compliance, Competitive landscape, etc.)
// changing shouldn't trigger a regeneration; only the requirements
// themselves should. Exported (not just used internally by
// hashRequirementsSection below) so testPlanIncremental.ts's
// per-requirement parsing reuses the exact same section-boundary logic
// rather than duplicating it.
function extractRequirementsSection(productDocContent) {
    const startIdx = productDocContent.indexOf(REQUIREMENTS_HEADING);
    if (startIdx === -1) {
        return "";
    }
    const afterHeading = startIdx + REQUIREMENTS_HEADING.length;
    const nextHeadingIdx = productDocContent.indexOf("\n## ", afterHeading);
    return nextHeadingIdx === -1 ? productDocContent.slice(afterHeading) : productDocContent.slice(afterHeading, nextHeadingIdx);
}
function hashRequirementsSection(productDocContent) {
    return (0, crypto_1.createHash)("sha256").update(extractRequirementsSection(productDocContent), "utf-8").digest("hex");
}
// Protects the path *and* the content, unlike productDoc.ts's banner —
// this file is fully machine-owned. A manual edit isn't preserved the
// way a docs/PRODUCT.md edit would be; it's overwritten the next time
// drift is detected, since there's nowhere for a hand-edit to "merge"
// into a regenerated document the way there is for the source doc.
function testPlanBanner(sourceHash) {
    return `<!-- devlore:test-plan source-hash:${sourceHash} -->
> **Do not move, rename, or edit this file.** Devlore generates and maintains this test plan automatically from \`docs/PRODUCT.md\`'s requirements — manual edits will be overwritten the next time Devlore detects the requirements have changed. To change what's tested, update \`docs/PRODUCT.md\` itself.`;
}
function extractSourceHash(content) {
    const match = content.match(TEST_PLAN_MARKER_PATTERN);
    return match ? match[1] : null;
}
// Returns null when missing (first-ever generation), not an empty-content
// sentinel — sync-test-plan.ts needs to tell "doesn't exist yet" apart
// from "exists but its hash doesn't match", and a plain null reads more
// clearly than an empty string standing in for both "no content" and "no
// sha to update against".
async function getExistingTestPlan(octokit, { owner, repo }) {
    try {
        const { data } = await octokit.rest.repos.getContent({ owner, repo, path: exports.TEST_PLAN_PATH });
        if (Array.isArray(data) || data.type !== "file") {
            return null;
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return null;
        }
        throw error;
    }
}
// Mirrors productDoc.ts's productDocPath/getDevloreProductDoc — the local
// chat server needs a copy of the test plan in the vault so it can serve
// the "Test plan" tab straight off disk (Requirement #11), the same way
// it already does for docs/PRODUCT.md. The project repo copy (above)
// stays the source of truth; this is a read-only mirror sync-test-plan.ts
// writes alongside it.
function testPlanVaultPath(projectName) {
    return `projects/${projectName}/test-plan.md`;
}
async function getDevloreTestPlan(octokit, { owner, repo, projectName }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: testPlanVaultPath(projectName),
        });
        if (Array.isArray(data) || data.type !== "file") {
            return { content: "" };
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return { content: "" };
        }
        throw error;
    }
}
function isNotFound(error) {
    return (typeof error === "object" &&
        error !== null &&
        "status" in error &&
        error.status === 404);
}
