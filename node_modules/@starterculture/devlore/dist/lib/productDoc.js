"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PRODUCT_DOC_MARKER = exports.SOURCE_PRODUCT_DOC_PATH = void 0;
exports.hasProductDocMarker = hasProductDocMarker;
exports.productDocBanner = productDocBanner;
exports.starterProductDoc = starterProductDoc;
exports.productDocPath = productDocPath;
exports.getSourceProductDoc = getSourceProductDoc;
exports.getDevloreProductDoc = getDevloreProductDoc;
// Mirrors a linked project's own docs/PRODUCT.md (intent, decisions,
// requirements) into Devlore, the same way log entries mirror diffs — so
// the local chat server can answer questions about it without ever
// needing a GitHub API call (Requirement #11). Source path is a fixed
// convention, not configurable per project, matching Devlore's own doc.
exports.SOURCE_PRODUCT_DOC_PATH = "docs/PRODUCT.md";
// Requirement #33: marks a docs/PRODUCT.md Devlore created/recognizes,
// distinguishing it from one that predates Devlore or was hand-authored
// without its conventions. The comment is invisible when rendered but
// reliably substring-matchable — unlike the visible banner text below,
// which a user could reasonably edit or trim over time without meaning
// to opt out of anything.
exports.PRODUCT_DOC_MARKER = "<!-- devlore:product-doc -->";
function hasProductDocMarker(content) {
    return content.includes(exports.PRODUCT_DOC_MARKER);
}
// Protects the *path*, not the content — the file is meant to be a living
// doc the user actively edits (discovery notes, requirements, decisions,
// reference material), including as source-of-truth context when working
// with an AI agent. What Devlore actually depends on is finding it at
// this exact convention path.
function productDocBanner() {
    return `${exports.PRODUCT_DOC_MARKER}
> **Do not move, rename, or delete this file.** Devlore depends on finding it at exactly \`docs/PRODUCT.md\` to build your project's documentation and answer questions about it. Its content is yours — use it as your living discovery/product doc, and as source-of-truth context when brainstorming or planning with an AI agent.`;
}
// Requirement #21/#33: starter content for a project that doesn't have a
// docs/PRODUCT.md yet — written for a brand-new repo (always missing) and
// for a first-time link to an existing repo that's missing one too.
function starterProductDoc(projectName) {
    return `${productDocBanner()}

# ${projectName}

Working name: ${projectName}. Add a one-line description of what this project is.

## Vision

What is this project trying to achieve? Fill this in.

## Requirements

Add hard requirements as they get locked in.

## Open questions

Track open questions here as they come up.
`;
}
function productDocPath(projectName) {
    return `projects/${projectName}/product.md`;
}
async function getSourceProductDoc(octokit, { owner, repo, ref }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: exports.SOURCE_PRODUCT_DOC_PATH,
            ref,
        });
        if (Array.isArray(data) || data.type !== "file") {
            return null;
        }
        return Buffer.from(data.content, "base64").toString("utf-8");
    }
    catch (error) {
        if (isNotFound(error)) {
            return null;
        }
        throw error;
    }
}
async function getDevloreProductDoc(octokit, { owner, repo, projectName }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: productDocPath(projectName),
        });
        if (Array.isArray(data) || data.type !== "file") {
            return { content: "" };
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return { content: "" };
        }
        throw error;
    }
}
function isNotFound(error) {
    return (typeof error === "object" &&
        error !== null &&
        "status" in error &&
        error.status === 404);
}
