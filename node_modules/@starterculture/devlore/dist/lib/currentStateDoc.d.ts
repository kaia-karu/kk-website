import type { Octokit } from "@octokit/rest";
export declare function currentStateDocPath(projectName: string): string;
interface CurrentStateDocParams {
    owner: string;
    repo: string;
    projectName: string;
}
interface CurrentStateDocFile {
    content: string;
    sha?: string;
}
export declare function getCurrentStateDoc(octokit: Octokit, { owner, repo, projectName }: CurrentStateDocParams): Promise<CurrentStateDocFile>;
export {};
