import type { Octokit } from "@octokit/rest";
import { type TokenUsage } from "./tokenUsage";
export type UsageSource = "draftLogEntry" | "consolidateRelease" | "analyzeCodebase" | "syncTestPlan" | "syncUserManual" | "releaseNotes" | "visualizer" | "onboardingGuide";
export interface ProjectUsage {
    draftLogEntry: TokenUsage;
    consolidateRelease: TokenUsage;
    analyzeCodebase: TokenUsage;
    syncTestPlan: TokenUsage;
    syncUserManual: TokenUsage;
    releaseNotes: TokenUsage;
    visualizer: TokenUsage;
    onboardingGuide: TokenUsage;
}
export declare const EMPTY_PROJECT_USAGE: ProjectUsage;
export declare function usagePath(projectName: string): string;
interface UsageParams {
    owner: string;
    repo: string;
    projectName: string;
}
export declare function getUsage(octokit: Octokit, params: UsageParams): Promise<ProjectUsage>;
export declare function getUsageSha(octokit: Octokit, params: UsageParams): Promise<string | undefined>;
export declare function addUsage(current: ProjectUsage, source: UsageSource, delta: TokenUsage): ProjectUsage;
interface RecordUsageParams {
    owner: string;
    repo: string;
    projectName: string;
    branch: string;
}
export declare function recordUsage(octokit: Octokit, params: RecordUsageParams, source: UsageSource, delta: TokenUsage): Promise<void>;
export {};
