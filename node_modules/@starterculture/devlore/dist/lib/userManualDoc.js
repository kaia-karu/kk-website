"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.USER_MANUAL_PATH = void 0;
exports.userManualBanner = userManualBanner;
exports.extractSourceHash = extractSourceHash;
exports.getExistingUserManual = getExistingUserManual;
exports.userManualVaultPath = userManualVaultPath;
exports.getDevloreUserManual = getDevloreUserManual;
// docs/USER_MANUAL.md is generated per linked project from that project's
// own docs/PRODUCT.md requirements — same architecture as docs/TEST_PLAN.md
// (testPlanDoc.ts), just aimed at an end user instead of a QA engineer.
// sync-user-manual.ts reuses testPlanDoc.ts's hashRequirementsSection()
// directly rather than duplicating it here: both only care about the
// ## Requirements section drifting, for the same reason (narrative
// sections like Vision or Legal & compliance changing shouldn't trigger a
// regeneration). Fully machine-owned, same as the test plan — the banner
// protects the path *and* the content, unlike docs/PRODUCT.md's "content
// is yours" banner.
exports.USER_MANUAL_PATH = "docs/USER_MANUAL.md";
const USER_MANUAL_MARKER_PATTERN = /<!-- devlore:user-manual source-hash:([a-f0-9]+) -->/;
function userManualBanner(sourceHash) {
    return `<!-- devlore:user-manual source-hash:${sourceHash} -->
> **Do not move, rename, or edit this file.** Devlore generates and maintains this user manual automatically from \`docs/PRODUCT.md\`'s requirements — manual edits will be overwritten the next time Devlore detects the requirements have changed. To change what's documented, update \`docs/PRODUCT.md\` itself.`;
}
function extractSourceHash(content) {
    const match = content.match(USER_MANUAL_MARKER_PATTERN);
    return match ? match[1] : null;
}
// Returns null when missing (first-ever generation), not an empty-content
// sentinel — mirrors getExistingTestPlan()'s exact reasoning.
async function getExistingUserManual(octokit, { owner, repo }) {
    try {
        const { data } = await octokit.rest.repos.getContent({ owner, repo, path: exports.USER_MANUAL_PATH });
        if (Array.isArray(data) || data.type !== "file") {
            return null;
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return null;
        }
        throw error;
    }
}
// Mirrors testPlanDoc.ts's testPlanVaultPath/getDevloreTestPlan — the
// local chat server needs a copy in the vault so the sidebar's "User
// Manual" pane can read it straight off disk (Requirement #11), the same
// way the "Test plan" tab already does.
function userManualVaultPath(projectName) {
    return `projects/${projectName}/user-manual.md`;
}
async function getDevloreUserManual(octokit, { owner, repo, projectName }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: userManualVaultPath(projectName),
        });
        if (Array.isArray(data) || data.type !== "file") {
            return { content: "" };
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return { content: "" };
        }
        throw error;
    }
}
function isNotFound(error) {
    return (typeof error === "object" &&
        error !== null &&
        "status" in error &&
        error.status === 404);
}
