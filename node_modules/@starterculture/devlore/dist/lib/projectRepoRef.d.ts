import type { Octokit } from "@octokit/rest";
export declare function projectRepoRefPath(projectName: string): string;
export interface ProjectRepoRef {
    owner: string;
    repo: string;
}
export interface VaultLocation {
    owner: string;
    repo: string;
}
interface ProjectRepoRefFile {
    ref: ProjectRepoRef;
    sha: string;
}
export declare function getProjectRepoRef(octokit: Octokit, vault: VaultLocation, projectName: string): Promise<ProjectRepoRefFile | null>;
export declare function resolveProjectRepoRef(octokit: Octokit, vault: VaultLocation, projectName: string): Promise<ProjectRepoRef>;
interface WriteProjectRepoRefParams {
    vault: VaultLocation;
    projectName: string;
    branch: string;
    target: ProjectRepoRef;
}
export declare function writeProjectRepoRef(octokit: Octokit, { vault, projectName, branch, target }: WriteProjectRepoRefParams): Promise<void>;
export {};
