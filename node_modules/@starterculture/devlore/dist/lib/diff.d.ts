import type { Octokit } from "@octokit/rest";
export interface DiffFile {
    filename: string;
    status: string;
    patch: string;
}
export interface ProjectDiff {
    commitMessages: string[];
    files: DiffFile[];
}
interface GetProjectDiffParams {
    owner: string;
    repo: string;
    base: string;
    head: string;
}
export declare function getProjectDiff(octokit: Octokit, { owner, repo, base, head }: GetProjectDiffParams): Promise<ProjectDiff>;
export {};
