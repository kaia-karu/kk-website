"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildCodebaseSnapshot = buildCodebaseSnapshot;
const promises_1 = require("fs/promises");
const path_1 = __importDefault(require("path"));
// Requirement #34: a lightweight, best-effort snapshot of a checked-out
// repo for the initial codebase analysis — the file tree for structural
// orientation, plus a small, deterministic set of high-signal files for
// substance. Deliberately not a "smart" sampling of source files: that
// would be non-deterministic and work against the analysis's own stated
// purpose (a baseline orientation snapshot, not exhaustive documentation).
const EXCLUDED_DIRS = new Set([
    "node_modules",
    ".git",
    "dist",
    "build",
    "coverage",
    ".next",
    ".turbo",
    ".cache",
    "target",
    "vendor",
    "venv",
    ".venv",
    "__pycache__",
    ".pytest_cache",
    "out",
]);
const HIGH_SIGNAL_MANIFESTS = [
    "package.json",
    "pyproject.toml",
    "Cargo.toml",
    "go.mod",
    "pom.xml",
    "build.gradle",
    "Gemfile",
    "composer.json",
];
const README_PATTERN = /^readme(\.|$)/i;
async function buildCodebaseSnapshot(rootDir) {
    const tree = await walk(rootDir, "");
    const files = await readHighSignalFiles(rootDir, tree);
    return { tree, files };
}
async function walk(rootDir, relDir) {
    const entries = await (0, promises_1.readdir)(path_1.default.join(rootDir, relDir), { withFileTypes: true });
    const results = [];
    for (const entry of entries) {
        const relPath = relDir ? `${relDir}/${entry.name}` : entry.name;
        if (entry.isDirectory()) {
            if (EXCLUDED_DIRS.has(entry.name)) {
                continue;
            }
            results.push(...(await walk(rootDir, relPath)));
        }
        else {
            results.push(relPath);
        }
    }
    return results.sort((a, b) => a.localeCompare(b));
}
function isHighSignal(filePath) {
    if (filePath === "docs/PRODUCT.md") {
        return true;
    }
    if (filePath.includes("/")) {
        return false;
    }
    return README_PATTERN.test(filePath) || HIGH_SIGNAL_MANIFESTS.includes(filePath);
}
async function readHighSignalFiles(rootDir, tree) {
    const candidates = tree.filter(isHighSignal);
    return Promise.all(candidates.map(async (filePath) => ({
        path: filePath,
        content: await (0, promises_1.readFile)(path_1.default.join(rootDir, filePath), "utf-8"),
    })));
}
