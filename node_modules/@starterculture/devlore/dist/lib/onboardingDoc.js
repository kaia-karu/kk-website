"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ONBOARDING_PATH = void 0;
exports.onboardingBanner = onboardingBanner;
exports.getExistingOnboardingGuide = getExistingOnboardingGuide;
exports.onboardingVaultPath = onboardingVaultPath;
exports.getDevloreOnboardingGuide = getDevloreOnboardingGuide;
// Requirement #48: a "start here" doc for a new contributor, generated
// per linked project the same dual-storage way as docs/TEST_PLAN.md and
// docs/USER_MANUAL.md — committed into the project repo itself (so it's
// discoverable directly on GitHub, before anyone's opened Devlore at
// all) and mirrored into the vault for the local chat server to read.
//
// No source-hash marker, unlike testPlanDoc.ts/userManualDoc.ts: this
// doc doesn't drift-detect against docs/PRODUCT.md's Requirements
// section at all — sync-onboarding.ts always regenerates at every
// release tag, since a real release is itself a meaningful "the world
// changed" event, the same reasoning consolidate-release.ts already
// applies to master docs. No skip-if-unchanged case to detect.
exports.ONBOARDING_PATH = "docs/ONBOARDING.md";
function onboardingBanner() {
    return `<!-- devlore:onboarding -->
> **Do not move, rename, or edit this file.** Devlore generates and maintains this onboarding guide automatically at every release — manual edits will be overwritten at the next one. To change what's documented, update the underlying sources (\`docs/PRODUCT.md\`, decisions, master docs) instead.`;
}
// Returns null when missing (first-ever generation), not an empty-content
// sentinel — mirrors getExistingTestPlan()'s exact reasoning.
async function getExistingOnboardingGuide(octokit, { owner, repo }) {
    try {
        const { data } = await octokit.rest.repos.getContent({ owner, repo, path: exports.ONBOARDING_PATH });
        if (Array.isArray(data) || data.type !== "file") {
            return null;
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return null;
        }
        throw error;
    }
}
// Mirrors testPlanDoc.ts's testPlanVaultPath/getDevloreTestPlan — the
// local chat server needs a copy in the vault so the "Onboarding" tab
// can read it straight off disk (Requirement #11), the same way Test
// Plan/User Manual/Visualizer already do.
function onboardingVaultPath(projectName) {
    return `projects/${projectName}/onboarding.md`;
}
async function getDevloreOnboardingGuide(octokit, { owner, repo, projectName }) {
    try {
        const { data } = await octokit.rest.repos.getContent({
            owner,
            repo,
            path: onboardingVaultPath(projectName),
        });
        if (Array.isArray(data) || data.type !== "file") {
            return { content: "" };
        }
        return { content: Buffer.from(data.content, "base64").toString("utf-8"), sha: data.sha };
    }
    catch (error) {
        if (isNotFound(error)) {
            return { content: "" };
        }
        throw error;
    }
}
function isNotFound(error) {
    return (typeof error === "object" &&
        error !== null &&
        "status" in error &&
        error.status === 404);
}
